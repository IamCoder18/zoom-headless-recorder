{
  "name": "zoombie-cli",
  "version": "1.0.0",
  "description": "CLI for automated ZoomPipe meeting recording",
  "main": "dist/index.js",
  "bin": {
    "zoombie": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "build:image": "cd .. && docker buildx build --platforms=linux/amd64,linux/arm64,linux/arm/v7 --push -t ghcr.io/$(gh api user --jq .login)/zoombie:latest .",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "ora": "^8.1.0",
    "inquirer": "^12.5.0"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.7",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}